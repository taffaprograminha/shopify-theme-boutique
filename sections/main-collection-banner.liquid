<section class="collection-banner">
  <div class="page-width">
    <h1>{{ collection.title }}</h1>
    {%- if collection.description != blank -%}
      <div class="collection-banner__desc rte">{{ collection.description }}</div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{ "name": "Collection banner", "settings": [] }
{% endschema %}
